mutation RetireFreeDate($input: RetireFreeDateInput!) {
  retireFreeDate(input: $input) {
    __typename
    ... on AuthError {
      message
    }
    ... on FreeDate {
      id
	  ...FreeDateCard
    }
    ... on Error {
      message
    }
  }
}