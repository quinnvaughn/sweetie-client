mutation UnretireFreeDate($input: UnretireFreeDateInput!) {
  unretireFreeDate(input: $input) {
    __typename
    ... on FreeDate {
		id
      ...FreeDateCard
    }
    ... on Error {
      message
    }
  }
}