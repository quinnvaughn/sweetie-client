query DateExperiences($query: String, $timesOfDay: [String!], $nsfw: String) {
  dateExperiences(query: $query, timesOfDay: $timesOfDay, nsfw: $nsfw) {
    edges {
      node {
        ...FreeDateCard
      }
    }
  }
}