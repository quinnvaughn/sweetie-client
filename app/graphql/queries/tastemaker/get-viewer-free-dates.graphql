query GetViewerFreeDates($retired: Boolean!) {
    viewer {
	  id
    tastemaker {
        id
        freeDates (retired: $retired) {
          id
          ...FreeDateCard
        }
      }
    }
  }