query GetViewerFreeDates($retired: Boolean!) {
    viewer {
	  id
    tastemaker {
        id
        experiences (retired: $retired) {
          id
          ...FreeDateCard
        }
      }
    }
  }