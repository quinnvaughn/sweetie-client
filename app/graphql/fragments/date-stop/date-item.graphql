fragment DateStopItem on DateStop {
    id
    title
    order
    content
    location {
      id
      name
      website
	  address {
		id
		formattedAddress
		coordinates {
			lat
			lng
		}
	  }
    }
  }